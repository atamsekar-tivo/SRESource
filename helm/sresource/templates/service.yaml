apiVersion: v1
kind: Service
metadata:
  name: {{ include "sresource.fullname" . }}
  labels:
    {{- include "sresource.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sresource.selectorLabels" . | nindent 4 }}